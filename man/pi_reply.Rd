% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pi_reply.R
\name{pi_reply}
\alias{pi_reply}
\alias{print.pi_reply}
\alias{as.character.pi_reply}
\title{The reply from a Pi agent turn}
\usage{
\method{print}{pi_reply}(x, ...)

\method{as.character}{pi_reply}(x, ...)
}
\arguments{
\item{x}{A \code{pi_reply}.}

\item{...}{Ignored.}
}
\value{
\code{print()} returns \code{x} invisibly; \code{as.character()} returns the plain
reply lines with attributes stripped.
}
\description{
\code{\link[=pi_run]{pi_run()}} and a session's \code{ask()} return a \code{pi_reply}: the agent's output as
a character vector (one element per line) carrying the \code{prompt} and \code{model}
as attributes, with a print method that writes the lines plainly. So a reply
prints as the agent wrote it — in an R console, a script, or a knitr chunk —
instead of as a quoted character vector.
}
\examples{
r <- structure(c("hello", "world"), class = "pi_reply")
r
as.character(r)
}
