% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pi_stream.R
\name{pi_stream}
\alias{pi_stream}
\title{Stream a live Pi agent turn as it happens}
\usage{
pi_stream(
  prompt,
  model = NULL,
  provider = NULL,
  extension = NULL,
  session = NULL,
  thinking = NULL,
  timeout = 300,
  dir = NULL,
  on_delta = function(text) cat(text),
  on_event = NULL,
  pi_bin = .pi_bin()
)
}
\arguments{
\item{prompt}{The user prompt (a single string).}

\item{model}{Model pattern or ID. Defaults to \code{getOption("piknit.model")},
then "gpt-5.3-codex-spark" — a small, fast model keeps a live turn to
seconds, so a whole document renders as a real integration test.}

\item{provider}{Provider id. Defaults to \code{getOption("piknit.provider")},
then "openai-codex".}

\item{extension}{Optional character vector of extension entrypoints, passed
as repeated \code{-e} flags (e.g. a Pi coding-agent extension that exposes tools).}

\item{session}{Optional session id. When set, turns that share the id share
conversation memory (a persistent session across chunks); when \code{NULL}
(default) the turn is ephemeral (\code{--no-session}). See \code{\link[=pi_session]{pi_session()}}.}

\item{thinking}{Optional thinking level (e.g. "high"), for models that support it.}

\item{timeout}{Seconds before the turn is killed (default 300).}

\item{dir}{Project root to start the agent in (its working directory). pi
treats the working directory as the project root, so \code{dir} is what scopes
the agent's file tools, and \code{extension}/skill/manifest paths are resolved
relative to it. The working directory is restored afterwards. \code{NULL}
(default) runs in the current directory.}

\item{on_delta}{Function called with each chunk of assistant text as it
streams (default writes it to the console with \code{\link[=cat]{cat()}}). \code{NULL} suppresses
live output.}

\item{on_event}{Optional function called with every parsed JSON event (a
named list), e.g. to react to \code{message_update} tool calls or \code{turn_end}.}

\item{pi_bin}{Path to the pi binary (default resolves \code{piknit.bin} /
\code{PIKNIT_PI} / "pi").}
}
\value{
A \link{pi_reply} with the full assistant text, accumulated from the deltas.
}
\description{
Runs one agent turn over pi's JSON-RPC mode (\verb{pi --mode rpc}) and delivers the
output \emph{incrementally} as it is produced, instead of waiting for the whole
turn like \code{\link[=pi_run]{pi_run()}}. Assistant text arrives as deltas (streamed to \code{on_delta}
by default), and every parsed event is available through \code{on_event} for
tool-call, thinking, or lifecycle handling. The turn ends on pi's \code{agent_end}
event. Needs the suggested packages \pkg{processx} and \pkg{jsonlite}.
}
\examples{
\dontrun{
# print the answer as it is typed
pi_stream("Explain content-addressed storage in two sentences.")
}
}
\seealso{
\code{\link[=pi_run]{pi_run()}} for a simpler blocking turn.
}
