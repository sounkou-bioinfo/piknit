% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/session.R
\name{pi_session}
\alias{pi_session}
\title{Open a persistent Pi agent session}
\usage{
pi_session(id = NULL, ...)
}
\arguments{
\item{id}{Session id. Defaults to a generated id.}

\item{...}{Defaults forwarded to \code{\link[=pi_run]{pi_run()}} for every turn (e.g. \code{model},
\code{provider}, \code{extension}, \code{thinking}, \code{timeout}).}
}
\value{
A list with elements \code{id}, \code{ask(prompt, ...)} (per-turn overrides win
over the session defaults), and \code{history()} (a list of \code{prompt}/\code{reply} pairs).
}
\description{
Returns a session object whose \code{ask()} method runs successive turns that
share one conversation via a stable \code{--session-id}, so the agent remembers
earlier turns across chunks — a "running Pi session" inside a document. No
long-lived process is held: each turn is a fresh, robust spawn bound to the
shared session id, so a hung turn cannot wedge the whole render.
}
\examples{
\dontrun{
s <- pi_session(extension = "extensions/pi-coding-agent/index.ts")
s$ask("Load examples/variant-counts/manifest.json and count variants by consequence.")
s$ask("Now break the same counts down as percentages.") # remembers the table above
}
}
\seealso{
\code{\link[=pi_run]{pi_run()}}
}
